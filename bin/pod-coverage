#!/usr/bin/env perl6

use v6;
use Pod::Coverage;

sub MAIN(Str $dir = './META.info', Bool :$anypod){
    for Pod::Coverage.use-meta($dir,$anypod) -> $res {
       say join("\n", $res.get-results>>.gist);
    }   
}
